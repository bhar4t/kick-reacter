<!--

Redux Boilerplate

import { useDispatch, useSelector} from 'react-redux'
import { doEntity } from '../redux';

function DashboardComponent() {
    console.log(useSelector(state => state.entity.arr))
    console.log(useDispatch()(doEntity()))
    return (
      <div className="dashboardContainer">
        Hello, World!
      </div>
    );
  }

export default DashboardComponent;

-->
